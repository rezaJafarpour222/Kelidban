fn main() {
    // let db = storage::database::Db::new("Kelidban").unwrap();
    // db.initialize().unwrap();
    // db.add_new_vault("Test", None).unwrap();
    // let vaults = db.get_vaults().unwrap();
    // println!("{:?}", db.get_entries_from_vault(vaults[0].id));
}
